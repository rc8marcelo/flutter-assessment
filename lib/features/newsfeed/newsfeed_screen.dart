import 'package:flutter/material.dart';

/*
  * use newsapi package to display list of news
  * use dio to get an article (details page)
  * store retrieved data locally using hive
  * pull to refresh (?)
*/

class NewsFeedScreen extends StatelessWidget {
  const NewsFeedScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
